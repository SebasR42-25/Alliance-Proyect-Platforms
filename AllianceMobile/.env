# ─── API del Backend NestJS ──────────────────────────────────────────────────
# Emulador Android:   http://10.0.2.2:3000/api   (ya es el default, puedes omitir)
# Simulador iOS:      http://localhost:3000/api   (ya es el default, puedes omitir)
# Dispositivo físico: descomenta y pon la IP de tu máquina en la red WiFi:
# EXPO_PUBLIC_API_URL=http://192.168.1.20:3000/api

# ─── Credenciales del Backend (NO se usan directo aquí — son del servidor) ───
# MongoDB Atlas, Cloudinary y NewsAPI viven en alliance-backend/.env
# La app móvil solo habla con el backend NestJS via REST/WebSocket
